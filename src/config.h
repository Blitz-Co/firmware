#pragma once

// Unique identifier of the device
#define DEVICE_ID "id-123"

// SSID and password of soft access point created to obtain the wifi credentials
#define AP_SSID DEVICE_ID " (config)"
#define AP_PWD "passwordd"

// Pin in which the current meter is connected
#define CURRENT_INPUT_PIN A0

// Debug mode
#define DEBUG true
